# Pinned toolchain for .github/workflows/validate.yml. Bump deliberately:
# a new pySigma ships a new bundled ATT&CK dataset, which changes what the
# (advisory) attacktag validator flags.
pysigma==1.5.0
sigma-cli==3.1.0
pySigma-validators-sigmahq==0.21.0
yamllint==1.37.1
PyYAML==6.0.3
